// jest-dom adds custom matchers for asserting on DOM nodes, e.g.
// expect(element).toHaveTextContent(/react/i)
// learn more: https://github.com/testing-library/jest-dom
//
// The '/vitest' entrypoint (not the root one, and not the v4 '/extend-expect'
// that no longer exists) is what also widens vitest's `expect` type, so the
// matchers typecheck inside .tsx tests.
import '@testing-library/jest-dom/vitest';
